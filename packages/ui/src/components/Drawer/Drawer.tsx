import React,{useEffect} from 'react';import './drawer.scss';
export function Drawer({open,onClose,title,children,side='right'}:{open:boolean;onClose:()=>void;title?:React.ReactNode;children:React.ReactNode;side?:'left'|'right'}){useEffect(()=>{if(!open)return;const h=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();addEventListener('keydown',h);return()=>removeEventListener('keydown',h)},[open,onClose]);if(!open)return null;return <div className="ui-drawer__backdrop" onMouseDown={onClose}><aside className={`ui-drawer ui-drawer--${side}`} onMouseDown={e=>e.stopPropagation()}><header><strong>{title}</strong><button onClick={onClose}>×</button></header><div>{children}</div></aside></div>}
