import React from 'react';import './table.scss';
export interface TableColumn<T>{key:keyof T|string;header:React.ReactNode;render?:(row:T)=>React.ReactNode;align?:'left'|'center'|'right';width?:string|number}
export function Table<T extends Record<string,unknown>>({columns,data,empty='No data'}:{columns:TableColumn<T>[];data:T[];empty?:React.ReactNode}){return <div className="ui-table-wrap"><table className="ui-table"><thead><tr>{columns.map(c=><th key={String(c.key)} style={{textAlign:c.align,width:c.width}}>{c.header}</th>)}</tr></thead><tbody>{data.length?data.map((row,i)=><tr key={i}>{columns.map(c=><td key={String(c.key)} style={{textAlign:c.align}}>{c.render?c.render(row):String(row[c.key as keyof T]??'')}</td>)}</tr>):<tr><td colSpan={columns.length} className="ui-table__empty">{empty}</td></tr>}</tbody></table></div>}
