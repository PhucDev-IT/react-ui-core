import React,{useEffect,useRef,useState} from 'react';import './dropdown.scss';
export function Dropdown({trigger,children,align='start'}:{trigger:React.ReactNode;children:React.ReactNode;align?:'start'|'end'}){const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const h=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};addEventListener('mousedown',h);return()=>removeEventListener('mousedown',h)},[]);return <div className="ui-dropdown" ref={ref}><div onClick={()=>setOpen(v=>!v)}>{trigger}</div>{open&&<div className={`ui-dropdown__menu ui-dropdown__menu--${align}`}>{children}</div>}</div>}
export function DropdownItem({children,onClick,danger}:{children:React.ReactNode;onClick?:()=>void;danger?:boolean}){return <button className={danger?'is-danger':''} onClick={onClick}>{children}</button>}
