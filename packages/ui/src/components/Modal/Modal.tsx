import React,{useEffect} from 'react';import {Button} from '../Button/Button';import './modal.scss';
export function Modal({open,onClose,title,children,footer,size='md'}:{open:boolean;onClose:()=>void;title:React.ReactNode;children:React.ReactNode;footer?:React.ReactNode;size?:'sm'|'md'|'lg'}){useEffect(()=>{if(!open)return;const h=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();addEventListener('keydown',h);return()=>removeEventListener('keydown',h)},[open,onClose]);if(!open)return null;return <div className="ui-modal__backdrop" onMouseDown={onClose}><section className={`ui-modal ui-modal--${size}`} role="dialog" aria-modal="true" onMouseDown={e=>e.stopPropagation()}><header><h3>{title}</h3><Button variant="ghost" size="sm" onClick={onClose}>×</Button></header><div className="ui-modal__body">{children}</div>{footer&&<footer>{footer}</footer>}</section></div>}
