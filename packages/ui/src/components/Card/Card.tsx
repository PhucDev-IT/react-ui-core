import React from 'react';import {cx} from '../../utils/cx';import './card.scss';
export function Card({title,description,actions,children,className,padding=true}:{title?:React.ReactNode;description?:React.ReactNode;actions?:React.ReactNode;children:React.ReactNode;className?:string;padding?:boolean}){return <section className={cx('ui-card',className)}>{(title||actions)&&<header className="ui-card__header"><div><div className="ui-card__title">{title}</div>{description&&<div className="ui-card__desc">{description}</div>}</div>{actions}</header>}<div className={cx('ui-card__body',!padding&&'ui-card__body--flush')}>{children}</div></section>}
